/** @jsxImportSource @emotion/react */
import { css }  from '@emotion/react'
import { space } from '../../../styles'

const styles = {
  main: css({
    maxWidth: `calc(${space} * 40)`,
    padding: `calc(${space} * 6)`,
  }),
}
export default styles
