/** @jsxImportSource @emotion/react */
import { css }  from '@emotion/react'
import { space } from '../../../styles'

const styles = {
  textbox: css({
    marginBottom: `calc(${space} * 2)`,
  }),
  button: css({
    margin: `calc(${space} * 20) 0`
  })
}
export default styles
