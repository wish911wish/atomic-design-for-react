import { line } from './color'

export const space = '0.5rem'

export const lineWidth = '1px'
export const lineStyle = 'solid'

export const border = `${lineWidth} ${lineStyle} ${line}`

export const radius = '2px'